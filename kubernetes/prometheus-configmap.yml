apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: sre-project
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: "order-service"
        static_configs:
          - targets: ["order-service:5000"]