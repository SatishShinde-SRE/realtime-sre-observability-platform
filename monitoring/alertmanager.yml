global:
  resolve_timeout: 5m

route:
  receiver: local-alerts
  group_by:
    - alertname
    - severity
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: local-alerts